import sys

def pibo(n:int)->int:
	#print(n)
	if n==1 :
		return 1
	elif n==0:
		return 0
	if memo[n] > 0 :
		return memo[n]
	memo[n] = pibo(n-1)+pibo(n-2)
	return memo[n]


n = int(sys.stdin.readline())
memo = [0 for i in range(n+1)]
memo[0] = 0
memo[1] = 1
print(pibo(n))